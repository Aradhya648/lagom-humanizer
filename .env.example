GEMINI_API_KEY=your_gemini_api_key_here
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# NVIDIA NIM — Tier 0 fingerprint break (free, OpenAI-compatible)
# Get your key at: https://build.nvidia.com  → "Get API Key"
# Without this key the NVIDIA pass is silently skipped (zero regression).
NVIDIA_API_KEY=nvapi-your_nvidia_api_key_here
#
# Default model: openai/gpt-oss-20b
#   → Live-benchmarked: 2.2s per chunk on this account's free tier
#   → GPT lineage — trained to produce natural, human-sounding prose
#   → 20B parameters — follows complex rewriting constraints reliably
#   → Fastest working model confirmed on this specific NVIDIA account
#
# Fallback if gpt-oss-20b is unavailable:
#   NVIDIA_MODEL=meta/llama-3.2-3b-instruct  (2.1s, slightly lower quality)
#
# DO NOT USE (confirmed 404 on this account's free tier):
#   nvidia/mistral-nemo-minitron-8b-8k-instruct, nv-mistralai/mistral-nemo-12b-instruct
#   writer/palmyra-creative-122b, microsoft/phi-4-mini-instruct (35s+ timeout)